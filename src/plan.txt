- Detect if a space is potentially going to be consumed by an operator
    - If so, update the operator, recrusively after marking the current space as updated.
- Otherwise, update the space as usual

- When moving a value into an already full space
- Recursively update the 




}3+2
^_ _
^{{